import { StyleSheet, Button, Text, View } from "react-native";

export const NavBarComponent = () => {
    return (
        <View>
            <Button
            title="rec"
            />
            <Button
            title="toPlay"
            />
        </View>
    );
};
